# Leave unset for local dev (uses the vite dev-server proxy to localhost:4000).
# In production, set to your deployed backend's public URL + "/api",
# e.g. VITE_API_BASE_URL=https://your-backend.onrender.com/api
VITE_API_BASE_URL=
