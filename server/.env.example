GEMINI_API_KEY=
PORT=4000

# Optional: directory for the runtime requests.json store. Leave unset for local
# dev (defaults to src/data/). In production on Render/Railway/Fly.io, set this
# to your attached persistent volume's mount path (e.g. /data) so request/message
# history survives redeploys.
DATA_DIR=
